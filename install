#!/bin/bash

# Update package list
sudo apt-get update

# Install python-pip
sudo apt-get install -y python-pip

# Install nose, behave, coverage, pexpect, selenium, phantomjs
sudo pip install -r requirements.txt

# Clean up downloaded archives
sudo apt-get clean
